{
  "name": "data",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "STW data pipeline: game export -> normalized, web-ready JSON.",
  "scripts": {
    "build": "tsx src/build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
