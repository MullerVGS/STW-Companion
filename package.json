{
  "name": "stw-companion",
  "private": true,
  "version": "0.1.0",
  "description": "STW Companion — a Fortnite: Save the World companion: Collection Book wiki/tracker (heroes, personnel, schematics, perks) + Hero Loadout planner. Data pipeline + interactive web app.",
  "workspaces": [
    "data",
    "web",
    "live-data"
  ],
  "scripts": {
    "daily:collect": "npm run collect --workspace live-data",
    "daily:update": "npm run update --workspace live-data",
    "daily:fixture": "npm run build:home:fixture --workspace live-data",
    "data:build": "npm run build --workspace data",
    "web:dev": "npm run dev --workspace web",
    "web:build": "npm run build --workspace web",
    "dev": "npm run data:build && npm run daily:fixture && npm run web:dev",
    "build": "npm run data:build && npm run daily:fixture && npm run web:build",
    "pages:build": "npm run data:build && npm run daily:update && npm run web:build",
    "typecheck": "npm run typecheck --workspace data && npm run typecheck --workspace live-data && npm run typecheck --workspace web",
    "verify": "npm run typecheck && npm run build",
    "docker:dev": "docker compose -p stw-dev -f docker-compose.dev.yml up --build",
    "docker:dev:down": "docker compose -p stw-dev -f docker-compose.dev.yml down",
    "docker:dev:logs": "docker compose -p stw-dev -f docker-compose.dev.yml logs -f",
    "docker:up": "docker compose -p stw up -d --build",
    "docker:down": "docker compose -p stw down",
    "docker:logs": "docker compose -p stw logs -f"
  },
  "engines": {
    "node": ">=20"
  }
}
