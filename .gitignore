# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# logs / os
*.log
.DS_Store
.env
.env.local

# --- Game extraction output (large, regenerable — DO NOT commit) ---
# Real BanjoBotAssets output goes here; regenerate via the extraction guide.
data/raw/assets.json
data/raw/schematics.json
data/raw/ExportedImages/
data/raw/images/

# Generated, web-ready data + icons (produced by `npm run data:build`)
web/public/data/
web/public/icons/

# ...but always keep the committed sample fixture so the pipeline runs out-of-the-box
!data/raw/sample.assets.json
docs/*
!docs/extraction.md
