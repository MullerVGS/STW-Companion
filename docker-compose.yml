services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: stw-collection-book:latest
    container_name: stw-collection-book
    ports:
      - "8088:80"
    restart: unless-stopped
